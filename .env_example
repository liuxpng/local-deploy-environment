# ============================================
# 通用配置
# ============================================
# 主域名（根据实际情况修改）
DOMAIN=localhost
# 时区
TZ=Asia/Shanghai

# ============================================
# Traefik 配置
# ============================================
# Traefik Dashboard 域名
TRAEFIK_DOMAIN=traefik.${DOMAIN}
# Traefik Dashboard 用户名
TRAEFIK_USER=admin
# Traefik Dashboard 密码（请修改）
TRAEFIK_PASSWORD=changeme
# Traefik Dashboard 密码哈希（使用 htpasswd 生成，或使用在线工具）
# 默认: admin:changeme
# 生成命令（推荐使用 bcrypt）: docker run --rm httpd:alpine htpasswd -nbB admin 你的密码
# 或使用 APR1: docker run --rm httpd:alpine htpasswd -nb admin 你的密码
# 在线生成: https://hostingcanada.org/htpasswd-generator/ (选择 bcrypt)
# 注意: 在 .env 文件中，$ 需要转义为 $$
TRAEFIK_PASSWORD_HASH=admin:$$2y$$05$$.EPqvLGUJWP0sAptH2gyiO4c73D/axGIcYp8PHWp/PcjNkdlG9BuO
# Let's Encrypt 邮箱（如果使用 HTTPS，请填写真实邮箱）
ACME_EMAIL=admin@example.com

# ============================================
# Gitea 配置
# ============================================
# Gitea 访问域名
GITEA_DOMAIN=git.${DOMAIN}
# Gitea HTTP 端口（容器内部）
GITEA_HTTP_PORT=3000
# Gitea SSH 端口（宿主机暴露）
GITEA_SSH_PORT=2222
# Gitea 管理员用户名
GITEA_ADMIN_USER=gitadmin
# Gitea 管理员密码（请修改）
GITEA_ADMIN_PASSWORD=changeme123
# Gitea 管理员邮箱
GITEA_ADMIN_EMAIL=admin@example.com

# ============================================
# PostgreSQL 配置
# ============================================
# 数据库名称
POSTGRES_DB=gitea
# 数据库用户
POSTGRES_USER=gitea
# 数据库密码（请修改）
POSTGRES_PASSWORD=changeme456
# PostgreSQL 版本
POSTGRES_VERSION=15-alpine

# ============================================
# 网络配置
# ============================================
# Docker 网络名称
NETWORK_NAME=gitea-network

# ============================================
# Gitea Actions 配置（CI/CD）
# ============================================
# 启用 Actions 功能
GITEA_ACTIONS_ENABLED=true
# Runner 注册令牌（首次启动后需要从 Gitea UI 生成）
# 生成路径：管理员设置 → Actions → Runners → 创建新的 Runner
GITEA_RUNNER_REGISTRATION_TOKEN=3qrLnduMH1RI5iPWN0IRSRKvvxc6RJp5iSuRczyJ
# Runner 名称
GITEA_RUNNER_NAME=default-runner
# Runner 标签（用于 workflow 中指定运行环境）
GITEA_RUNNER_LABELS=ubuntu-latest:docker://catthehacker/ubuntu:act-latest

# ============================================
# Gitea Container Registry 配置（镜像仓库）
# ============================================
# 启用 Packages 功能（包含容器镜像仓库）
GITEA_PACKAGES_ENABLED=true
# 镜像仓库域名（与 Gitea 相同）
GITEA_REGISTRY_DOMAIN=${GITEA_DOMAIN}
